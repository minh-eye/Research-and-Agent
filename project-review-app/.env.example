# Anthropic API 키 (필수) — 서버(프록시)에만 보관되며 브라우저로 전송되지 않는다.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# 앱 비밀번호 게이트 (선택, 기본값 AISTP26) — 값을 빈 문자열로 두면 게이트를 끈다.
APP_PASSWORD=AISTP26

# 로컬 서버 포트 (선택, server.js 전용, 기본값 8787)
PORT=8787
